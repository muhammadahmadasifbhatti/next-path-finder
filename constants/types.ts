export type QuestionType = {
  question: string;
  answer: string;
};

export type NavigationItemType = {
  name: string;
  route: string;
  default?: boolean;
};

export type DimensionsType = {
  x: number;
  y: number;
};
