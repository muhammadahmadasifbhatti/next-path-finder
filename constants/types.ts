export type QuestionType = {
  question: string;
  answer: string;
};

export type NaigationItemType = {
  name: string;
  route: string;
  default?: boolean;
};

export type DimensionsType = {
  x: number;
  y: number;
};
